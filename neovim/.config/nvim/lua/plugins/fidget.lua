return {
   "j-hui/fidget.nvim",
   lazy = true,
   -- I don't need any options, but for some reason fidget doesn't work without
   -- it.
   opts = {},
   -- Disable the stupid startup warning about a pending rewrite
   tag = "legacy",
}
